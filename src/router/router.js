"use strict"

import Artist from '../modules/artist';
import ArtistForm from '../modules/artist-form';
import RelatedArtist from '../modules/related-artists'

const hash = window.location.hash.replace('#', '');

export default class Router {

logHash() {
  // if(hash === "artist:name") {
  //
  // }
  console.log(hash);
}


}
